import styled from "@emotion/styled";

export const REGISTRY_PATH = "config.traits";

export const AddArea = styled.div`
    display: flex;
    justify-content: flex-end;
    margin-top: 8px;
`;

export const EmptyMsg = styled.p`
    opacity: 0.5;
    font-size: 12px;
    text-align: center;
    padding: 16px 0;
`;

