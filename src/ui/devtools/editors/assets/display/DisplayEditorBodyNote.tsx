import { NoteSurface } from "./DisplayEditor.styles";

export const DisplayEditorBodyNote = () => (
    <NoteSurface>
        This display type has no additional authored fields.
    </NoteSurface>
);
