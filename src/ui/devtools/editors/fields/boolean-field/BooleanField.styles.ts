import styled from "@emotion/styled";
import { Label as SharedLabel } from "../Shared.styles";

export const Checkbox = styled.input`
    cursor: pointer;
    margin-left: 8px;
`;

export const CursorLabel = styled(SharedLabel)`
    cursor: pointer;
`;
