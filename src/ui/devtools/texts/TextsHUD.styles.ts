import styled from "@emotion/styled";
import {
    HudActions,
    HudBadge,
    HudCard,
    HudSubtitle,
} from "../layout/LayoutHUD.styles";

export const TextsHudCard = styled(HudCard)`
    position: sticky;
    top: 24px;
    margin-left: auto;
    z-index: 2;
`;

export const TextsHudBadge = styled(HudBadge)``;
export const TextsHudSubtitle = styled(HudSubtitle)``;
export const TextsHudActions = styled(HudActions)``;
