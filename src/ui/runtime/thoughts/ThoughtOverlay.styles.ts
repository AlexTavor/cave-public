import styled from "@emotion/styled";

export const ThoughtOverlayBody = styled.div`
    display: flex;
    flex-direction: column;
    gap: 16px;
    width: min(720px, 100%);
`;

export const ThoughtActions = styled.div`
    display: flex;
    justify-content: flex-end;
`;
